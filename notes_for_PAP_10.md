### Task Description:

#10 - frontend routing for the tools (#5)
- Replace the text input field with a name search/query parameter in the url. Show only those tools, which name contains the name filter. For example /tools?name=pe should show the Pencil and Pen tools. The search can be case sensitive or insensitive.
- Add a new page /tools/:id, where one tool and its details (name, weight) is shown only. Initiate a new fetch from the page based on a given path param in the url.


### Tech concepts:

- FE 
- 

### Notes:
